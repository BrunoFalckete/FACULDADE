export const protectedRoutes = ['/feed', '/feed/[id]']

export const authRoutes = ['/auth/login', '/auth/register']

export const notRenderNavBarRoutes = ['/auth/login', '/auth/register']

export const publicRoutes = [...authRoutes, ]